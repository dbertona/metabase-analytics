---
description: Contexto de arquitectura de superset-analytics — leer PRIMERO en cada chat
alwaysApply: true
---

# Arquitectura — superset-analytics

## 🎯 ¿Para qué sirve este repo?

Réplica de datos BC en **Apache Superset** para reporting financiero (Seguimiento Económico, Facturación P/R).
No contiene workflows n8n ni código de la app Timesheet/Gastos.

**Doc maestra:** `docs/GUIA_COMPLETA_ANALYTICS.md`  
**Troubleshooting P/R:** `docs/shared/analytics/ANALYTICS_FACTURACION_PBI_ALIGNMENT.md`  
**Workflow 004 (sync):** `docs/shared/analytics/004_SYNC_BC_ANALYTICS.md`

---

## 📍 Infraestructura

| Componente | Ubicación | Acceso |
|------------|-----------|--------|
| **Superset** | VM 100 — `http://192.168.36.100:8088/` | admin / ver `.env` |
| **PostgreSQL Analytics** | VM 100 — `192.168.36.100:5433` | postgres / `SuperSecurePassword2025` |
| **n8n prod (sync 004)** | VM 101 — `https://apps.powersolution.es/n8n/` | No usar n8n VM 100 (retirado) |

```bash
# Conectar a analytics DB desde terminal
docker run --rm --network host postgres:15 \
  psql "postgresql://postgres:SuperSecurePassword2025@192.168.36.100:5433/postgres"
```

---

## 📦 Responsabilidades por repo

| Repo | Qué contiene | ¿Tocar desde aquí? |
|------|-------------|-------------------|
| **superset-analytics** (este) | Spec PBI, exports Superset, docs dashboards, SQL vistas (canónico) | ✅ Sí |
| **power-solution-apps** | App Timesheet/Gastos (fuera de alcance en este workspace) | ❌ No (otro workspace) |
| **power-solution-docs** | Docs compartidas (`docs/shared/`) | Solo via submódulo |

---

## 🗄️ Tablas principales (Analytics DB)

Prefijo `bc_` — origen: workflow n8n 004 desde BC Production.

| Tabla | Descripción |
|-------|-------------|
| `bc_job_ledger_entry_month` | Movimientos reales agregados por mes (**Tipo R**) |
| `bc_job_planning_line` / `bc_planificacion_mes` | Planificación por mes (**Tipo P**) |
| `bc_expediente_mes` | Expedientes planificados (**Tipo P**) |
| `bc_meses_cerrados` | Meses bloqueados (cambia R↔P) |
| `bc_job` | Proyectos |
| `bc_resource` | Recursos humanos |
| `bc_department` | Departamentos |

## 📊 Vistas principales

| Vista | Descripción |
|-------|-------------|
| `v_se_facturacion` | UNION ALL P+R — fuente principal de Superset (**facturación canónica**) |
| `v_se_coste` | Capa Coste P/R (`SUM(coste)`); independiente; no sustituye facturación |
| `v_se_lineas_planificacion` | Tipo P (plan) con lógica vigente + Distinct PBI |
| `v_se_lineas_expedientes` | Tipo P (expedientes) |
| `v_se_lineas_movimientos` | Tipo R (movimientos reales) |
| `bi_v_*` | Vistas BI específicas para dashboards Superset |

---

## 🎯 KPIs de referencia (PSI 2026, jul 2026)

| KPI | Valor | Fuente |
|-----|-------|--------|
| Tipo P facturado | 3.712.450 € | `v_se_facturacion WHERE tipo='P'` |
| Tipo R facturado | 2.604.816 € | `v_se_facturacion WHERE tipo='R'` |
| Tipo P coste (objetivo PBI) | 3.788.848 € | `v_se_coste WHERE tipo='P'` (alineación en curso) |
| Tipo R coste (objetivo PBI) | 2.271.735 € | `v_se_coste WHERE tipo='R'` (alineación en curso) |

---

## 🔴 REGLAS CRÍTICAS

- **SQL Analytics (`v_se_*`, `bi_v_*`, helpers)** → fuente de verdad en este repo (`sql/views/`, `scripts/sql/`)
- **Workflow 004** → se opera contra n8n VM 101; la documentación funcional vive en este repo
- **Commits**: siempre rama `feat/` o `fix/` — nunca en `main` directamente
- **Push**: siempre `gitea`, nunca `origin` (GitHub es solo mirror)
- **Submodulo `docs/shared`**: actualizar con `git pull origin main` desde `docs/shared/` y commitear el puntero en el padre

---

## 📂 Estructura del repo

```text
superset-analytics/
  docs/
    GUIA_COMPLETA_ANALYTICS.md       ← referencia principal
    ACTUALIZAR_WORKFLOW_004.md        ← cómo actualizar el sync
    seguimiento-economico/README.md   ← especificación de dashboards
    shared/                           ← submódulo power-solution-docs
      analytics/
        004_SYNC_BC_ANALYTICS.md      ← arquitectura workflow 004
        ANALYTICS_FACTURACION_PBI_ALIGNMENT.md ← troubleshooting P/R
  sql/
    views/                            ← DDL de vistas SQL
  scripts/                            ← scripts operativos
  docker/                             ← config Superset Docker
```
